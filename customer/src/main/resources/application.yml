server:
  port: 9090

spring.application:
  name: CUSTOMER-SERVICE

spring.jpa:
  database: POSTGRESQL
  hibernate:
    ddl-auto: update
    show-sql: true

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.124.51:5432/customers
    username: postgres
    password: Welcome


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost